This file was written by an agent.

# Classification decisions

Jev was consulted through the keyring-backed classifier with the owner's explicit consent. Questions asked which class the supplied facts belonged to. Jev did not approve publication, permission changes or a release.

Probabilities describe the classifier's distribution over the offered labels. They are not measured defect rates or probabilities of release success. Every question included `none` and `need_more_context`. The [machine-readable record](classifications.json) retains every returned probability and confidence.

## Extension scope

Native extensions use a registration directory and explicit trust/activation; the existing docs described only plugin links. The test fixture installed duplicate QML below the working checkout. The popout could be exercised only through its qualification host.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| extension_docs | `deployment_paths_gap` | 1.0 | 1.0 |
| gate_failure | `owned_fixture_contamination` | 1.0 | 1.0 |
| popout_evidence | `qualified_component_only` | 1.0 | 1.0 |

## Collision

A real copy collision reached the history controller, but the Files pane did not instantiate the existing ActionDialog. Reusing that dialog restored a visible choice without new infrastructure.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| defect | `blocking_missing_ui_wiring` | 1.0 | 1.0 |
| repair | `reuse_existing_dialog` | 1.0 | 1.0 |

## Sftp proof

SFTP accepts discovered Tailscale peers. No authorized peer, SSH username or sample directory was supplied. A loopback simulation would not prove this remote workflow.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| sftp_prerequisite | `missing_user_target` | 1.0 | 1.0 |

## Final evidence scope

Desktop role recordings showed registration rather than completed handoff. Plugin recording showed install without activation. Recovery proof would seed a documented interrupted-operation fixture. A clean combined snapshot included concurrent changes; capture scripts contained session-specific host paths.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| role_coverage | `registration_only` | 1.0 | 1.0 |
| plugin_coverage | `installation_only` | 1.0 | 1.0 |
| recovery_fixture | `documented_fixture_proof` | 1.0 | 1.0 |
| combined_gate | `coherent_combined_validation` | 0.97 | 0.96 |
| harness_delivery | `owned_scratch` | 1.0 | 1.0 |

## Capture correction

The user required wallpaper, the full SVG wordmark, native-resolution captures, a blade no wider than one-third, and equal visible-edge padding. The initial classification of obsolete captures was weak and contradicted their measured defects; it was superseded by the next classification.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| evidence_status | `recapture_required` | 0.57 | 0.42 |
| new_geometry | `meets_requested_geometry` | 0.96 | 0.95 |

## Capture standard

Old recordings were 1600×900 with an icon-only watermark and a 650px blade, exceeding one-third. New recordings are 2560×1440 with the full original SVG artwork and a 650px blade, with wallpaper visible. Final padding measurement remained pending.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| old_evidence | `fails_new_explicit_requirements` | 1.0 | 1.0 |
| new_sample | `compliant_except_final_padding_recheck` | 0.98 | 0.97 |
| remaining_delivery | `incomplete` | 0.94 | 0.91 |

## Reinstall timing

A combined candidate was fully tested and staged. Installation drains the active runtime and restarting the shell reloads the visible desktop. The earlier restart request conflicts with the later instruction not to disrupt the session.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| timing_constraint | `timing_conflict` | 1.0 | 1.0 |

## Release readiness

The combined snapshot passed 760 Rust and 832 QML checks plus bundle/scaffold gates. Some promised recordings, real SFTP and complete integration handoffs remained pending. The dirty branch had no final main SHA verification; native distribution qualification remained open. Five workflow/pane groupings cover 112 documented workflows.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| release_state | `locally_validated_but_release_blocked` | 1.0 | 1.0 |
| docs_segmentation | `coherent_user_workflow_structure` | 0.91 | 0.87 |
| remaining_proof | `bounded_partial_evidence` | 1.0 | 1.0 |

The corrected capture-standard classification supersedes the weak first capture answer. Observed dimensions and source frames take precedence over a contradictory classifier result.

## Picker after media browsing

The unrestricted picker hid valid text files because the media grid remained active. Jev classified this as `interaction_defect` (1.00) and the existing-state guard as `reuse_existing_state_boundary` (1.00). Both questions assigned 0.00 to every alternative, including `none` and `need_more_context`. The subsequent QML lint and all 832 QML checks passed; live evidence is tracked on the picker page.

## Reveal selection

A real FileManager1 ShowItems request lost its explicit file selection to initial tree focus before the row loaded. Jev classified the defect as `selection_focus_race` (1.00) and the correction as `shared_state_guard` (1.00). The guard passed all 832 QML checks and a real D-Bus handoff now selects the requested file.

## Named tmux sockets

A real terminal attached with `tmux -L` was recognized as tmux but pane creation contacted the default server. Jev classified the defect as `named_socket_discovery` (1.00; confidence 0.99) and the repair as `reuse_native_metadata` (1.00). Discovery now preserves the client socket selection and uses tmux’s reported socket path. Real PTY regressions cover named, joined, explicit and relative socket paths, including a custom temporary directory.

## Review before Omen verification

The final gate passed 761 Rust and 832 QML checks, scaffolds, theme reload and reproducible bundle verification. The x86_64 package and native archive were built and verified locally. Coverage contains 112 guides and 111 accepted screenshot/MP4 pairs; SFTP is pending. Captures meet the measured geometry and branding standard, and all owned capture processes have been stopped. Final release identity, production integration qualification and authorization-dependent work remain open.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| release_state | `locally_validated_release_blocked` | 1.00 | 1.00 |
| evidence_completeness | `111_verified_one_missing_with_bounded_claims` | 0.99 | 0.98 |
| structure | `coherent_workflow_and_pane_groups` | 1.00 | 1.00 |
| branding | `meets_measured_requirements` | 0.93 | 0.91 |

The branding alternatives were geometry/branding failure 0.06 and insufficient context 0.01. The actual source frames and measured 32-pixel padding remain the evidence; classifier confidence does not replace those measurements. These results superseded the earlier pending-padding and incomplete-review states; the Omen update below supersedes the missing-SFTP classification.

## Omen verification and alias retention

The user explicitly named Omen. Actual tailnet discovery, a 4 ms ping, SSH authorization and read-only SFTP access succeeded. A UI attempt then exposed a real defect: refreshing locations discarded a connection made through the configured SSH alias because its canonical URI used the short alias while discovery used the full hostname.

Jev classified the defect as `alias_retention_bug` (1.00) and the minimal repair as `consistent_validated_host_matching` (0.99; the unvalidated-host alternative received 0.01). The repair retains the stable peer ID check and accepts only the currently discovered canonical host or its validated SSH alias. Canonical IPv6 matching is preserved.

The corrected candidate passed the complete gate: 761 Rust and 832 QML checks, scaffolds, theme reload and byte-identical static bundle verification. A separate live regression passed connect → refresh → list → disconnect on Omen. The native pane displayed its real `/usr/share/doc` tree; the backend returned 230 entries. The 5.97-second original-speed clip and screenshot passed media validation and visual review. Paired real compositor frames measured exactly 32 pixels of visible bottom and right padding.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| evidence | `complete_bounded_catalog` | 1.00 | 1.00 |
| release | `locally_validated_release_blocked` | 1.00 | 1.00 |
| grouping | `integrations_with_tailnet` | 1.00 | 1.00 |

All alternatives for these three questions, including `none` and `need_more_context`, received 0.00. This closes the catalogue’s final missing media pair: 112/112 workflows now have reviewed evidence. Remaining release identity, production qualification and desktop-installation gates still apply. The clip proves opening an already authorized read-only SFTP connection; other remote operations and other peers are outside its claim.

## Production integration update

The real installed Omarchy plugin now loads an extension through FileBlade's
own bar widget and the self-scoped host API. Add, activate, disable, update,
remove and a fresh shell restart passed. The shared popout now focuses its
module; actual bar click, Escape, outside click, repetition, invalid-module
refusal and unloading passed. Native and plugin shortcuts both work.

GIO cold-launched the installed autostart entry and opened a folder in the
native view. D-Bus automatically activated FileManager1 for Reveal and the
actual desktop portal/backend for the chooser; the chooser returned response
0 and the chosen file URI. All desktop roles reversed cleanly.

| Question | Chosen class | Probability | Confidence |
| --- | --- | ---: | ---: |
| repair_scope | `own_plugin_bar_entry` | 1.00 | 1.00 |
| production_scope | `production_paths_exercised` | 1.00 | 1.00 |
| autostart_claim | `desktop_entry_launch` | 0.98 | 0.97 |
| popout_group | `plugin_integration` | 1.00 | 1.00 |

Autostart's registration-only alternative received 0.02; full-login and
insufficient-context alternatives received 0.00. Every alternative to the
other chosen labels received 0.00. These results supersede the earlier
component-only popout, registration-only desktop-role and installation-only
plugin classifications. Native cross-process bar integration and a full login
transition are not claimed. Exact public-commit verification and publication
remain owner-controlled steps.
